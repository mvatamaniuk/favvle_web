import { Popper } from '@mui/material'
import { styled } from '@mui/material/styles'

export const PopperStyled = styled(Popper)(() => ({
  background: '#EAF8F8',
  boxShadow: '0px 3px 10px 1px rgba(21, 106, 108, 0.5)',
  padding: 10,
  borderRadius: 23,
}))
